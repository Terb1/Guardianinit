package viewsmodels

class DeviceRegister {}
/*
    init {
        direction()
    }

    private val recoderRegister = RecorderRegister()
    private val alarmRegister = AlarmRegister()
    var tipo: String = ""

    private fun direction(): String {
        if (deviceType() == "Recorder") {
            recoderRegister.PostRecorder()
            return (recoderRegister.PostRecorder())

        } else if (deviceType() == "Alarm") {
            alarmRegister.PostAlarm()
            return (alarmRegister.PostAlarm())

        }
        return "Erro"
    }

    fun deviceType(): String {
        if (readType("preencha o campo Tipo: ")) {
            if (tipo != "recorder" || tipo != "alarm") {
                println("Preecha apenas com recorder ou alarm")
            }
        }
        return tipo
    }


    fun readType(msg: String): Boolean {
        do {
            print(msg)
            tipo = readln().trim()
        } while (tipo.isEmpty())
        return true
    }
}
*/


